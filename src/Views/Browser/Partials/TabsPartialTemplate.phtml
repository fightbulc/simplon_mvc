<?php
/**
 * @var FlashMessage $flash
 * @var Locale $locale
 *
 * @var string $activeTabId
 * @var array $tabs
 * @var string $content
 */
use Simplon\Locale\Locale;
use Simplon\Mvc\Utils\CastAway;
use Simplon\Mvc\Views\Browser\FlashMessage;

?>
<div class="tiny-screen-container small-screen-container">
    <div class="ui grid">
        <?= $content ?>
    </div>
</div>

<div class="big-screen-container">
    <div class="ui top attached <?= CastAway::intToColumnWord(count($tabs)) ?> item tabular menu">
        <?php foreach ($tabs as $id => $tab): ?>
            <a href="<?= $tab['url'] ?>" class="item <?= $id === $activeTabId ? 'active' : null ?>">
                <?= $tab['label'] ?>
            </a>
        <?php endforeach ?>
    </div>
    <div class="ui bottom attached segment">
        <?= $content ?>
    </div>
</div>